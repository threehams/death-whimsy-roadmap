div
  .total-progress-container
    span.total-progress-title {{vm.progress | number: 0}}%
    progress.total-progress-bar(min=0, max=100, value='{{vm.progress}}')
    div.total-progress-description(ng-if='vm.done') Movement and controls are in a state where the first six levels can
      | be played start to finish!
  .code-canvas-container
    canvas.code-canvas(width=1000, height=563)
